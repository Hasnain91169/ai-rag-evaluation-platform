<!DOCTYPE html>
<html>
  <head>
    <title>Rails Hosted RAG Eval Platform</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <style>
      body { font-family: Arial, sans-serif; margin: 0; background: #f7f7f9; color: #111827; }
      nav { background: #111827; color: white; padding: 12px 16px; }
      nav a { color: white; text-decoration: none; margin-right: 14px; }
      .container { max-width: 1100px; margin: 20px auto; padding: 0 16px; }
      .card { background: white; border-radius: 8px; padding: 16px; margin-bottom: 16px; box-shadow: 0 1px 2px rgba(0,0,0,0.07); }
      table { width: 100%; border-collapse: collapse; }
      th, td { border-bottom: 1px solid #e5e7eb; padding: 8px; text-align: left; vertical-align: top; }
      .notice { background: #dcfce7; border: 1px solid #86efac; padding: 10px; border-radius: 8px; margin-bottom: 12px; }
      .alert { background: #fee2e2; border: 1px solid #fca5a5; padding: 10px; border-radius: 8px; margin-bottom: 12px; }
      .tag { display: inline-block; padding: 2px 8px; border-radius: 999px; background: #e5e7eb; font-size: 12px; }
      .tag-retrieval_issue { background: #fecaca; }
      .tag-ranking_issue { background: #fed7aa; }
      .tag-prompting_issue { background: #fde68a; }
      .tag-ok { background: #bbf7d0; }
      .mono { font-family: monospace; font-size: 12px; }
      textarea, input[type='text'], select { width: 100%; padding: 8px; border: 1px solid #d1d5db; border-radius: 6px; margin-bottom: 8px; }
      input[type='submit'], button { background: #111827; color: white; border: none; border-radius: 6px; padding: 8px 12px; cursor: pointer; }
      .small { color: #6b7280; font-size: 12px; }
    </style>
  </head>

  <body>
    <nav>
      <a href="<%= root_path %>">Dashboard</a>
      <a href="<%= new_document_path %>">Ingest Document</a>
      <a href="<%= documents_path %>">Documents</a>
      <a href="<%= new_query_trace_path %>">Ask Question</a>
      <a href="<%= query_traces_path %>">Traces</a>
      <a href="<%= eval_runs_path %>">Eval Runs</a>
    </nav>

    <div class="container">
      <% if flash[:notice] %>
        <div class="notice"><%= flash[:notice] %></div>
      <% end %>
      <% if flash[:alert] %>
        <div class="alert"><%= flash[:alert] %></div>
      <% end %>

      <%= yield %>
    </div>
  </body>
</html>
